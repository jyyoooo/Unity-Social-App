import 'package:flutter/material.dart';

Widget version() {
  return const Column(
    children: [
      Text(
        'version 1.2',
        style: TextStyle(color: Colors.grey, fontSize: 12),
      ),
      // Text(
      //   'Made with ♡︎ by jyyoooo',
      //   style: TextStyle(color: Colors.grey, fontSize: 12),
      // ),
    ],
  );
}
